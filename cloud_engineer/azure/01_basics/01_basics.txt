AZURE CLI

az --version		- version of azure
az login

az account show
az account list						- similar as above.
az account show --output json		- this is the default

az account show --output yaml		- outputs in yaml format.
az account show --output table		- outputs as table format

az find secret
az vm list



az account show
az account show --output json # JSON is the default format
az account show --output yaml
az account show --output table


A web browser has been opened at https://login.microsoftonline.com/organizations/oauth2/v2.0/authorize. 
Please continue the login in the web browser. If no web browser is available or if the web browser fails to open, use device code flow with `az login --use-device-code`.



To sign in, use a web browser to open the page https://microsoft.com/devicelogin and enter the code FH75EXTNA to authenticate.

az group list
